import { ModalOverlay, ModalWindow } from './ModalElements';

export const Modal = () => {
  return (
    <ModalOverlay>
      <ModalWindow>
        <img src="" alt="" />
      </ModalWindow>
    </ModalOverlay>
  );
};
